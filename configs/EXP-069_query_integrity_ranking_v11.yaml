experiment: EXP-069
protocol_revision: v1.1
correction: coverage_accounting_only_after_v10_zero_visible_source_abort
seed: 69027

carrier:
  repository: Open-d4rt
  model_config: Open-d4rt/checkpoints/OpenD4RT_32CLIP_9Dataset_NoAUG/model.yaml
  checkpoint: Open-d4rt/checkpoints/OpenD4RT_32CLIP_9Dataset_NoAUG/opend4rt.ckpt
  clip_frames: 32
  image_size: [256, 256]

data:
  root: Open-d4rt/data/worldtrack_release/adt_mini
  manifest: revisit3d/manifests/adt_cross_clip_exp068_v10.json
  role: validation
  inherited_manifest_experiment: EXP-068
  role_content_accessed_before_registration: false
  terminal_access: false

query:
  clips:
    reference: {start: 0, end: 32}
    large_shift: {start: 16, end: 48}
    adjacent_shift: {start: 1, end: 33}
  source_global_frame: 16
  target_global_frames: [20, 24, 28]
  maximum_tracks_per_sequence: 512
  alignment_fraction: 0.5
  split_seed: 6801010
  chunk_size: 2048

alignment:
  minimum_alignment_points: 32
  depth_layer:
    layers: 4

metric:
  apd_relative_thresholds: [0.01, 0.02, 0.04, 0.08]
  pair_knn: 4
  pair_max_distance_scene_fraction: 0.30

statistics:
  bootstrap_samples: 10000
  bootstrap_seed: 69069

success:
  maximum_replay_abs_difference: 1.0e-6
  exact_attempted_sequences: 11
  minimum_evaluable_sequences: 9
  minimum_large_over_adjacent_positive_fraction: 0.80
  minimum_layer_positive_fraction: 1.0
  minimum_mean_signed_apd_gain: 0.0
  minimum_signed_apd_gain_ci_lower: 0.0
  minimum_apd_positive_sequence_fraction: 0.50
  minimum_apd_positive_target_fraction: 0.40
  maximum_absolute_target_spearman: 0.30

output:
  result: revisit3d/results/EXP-069/query_integrity_ranking_v11.json
