# Results Policy

Official compact interpretations live in `docs/experiments/`. Lightweight machine-readable outputs currently live in `revisit3d/results/`.

Checkpoints, datasets, tensor dumps, and large logs are intentionally excluded from Git. Every reported experiment must link its raw artifact location and preserve the configuration needed to reproduce it.
